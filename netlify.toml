[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # This tells Netlify to use the latest internal Next.js integration
  NETLIFY_NEXT_PLUGIN_SKIP = "false"
